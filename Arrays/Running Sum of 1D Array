class Solution {
    public int[] runningSum(int[] nums) {
        int n = nums.length;
        int[] res = new int[n];

        for(int i = 0 ; i < n ; i++){
            if(i==0)res[i]=nums[i];
            else res[i] = res[i-1]+nums[i];
        }
        return res;
    }
}